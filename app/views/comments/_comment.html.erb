<div id="<%= dom_id(comment) %>">
<%= comment.body%><br>
by <%= comment.user.first_name%>
<% if can? :manage, @comment %>
<%= link_to "Delete", comment_path(comment), remote: true, method: :delete, data:{confirm: "Are you sure?"} %>
<% end %>
<hr>
</div>
